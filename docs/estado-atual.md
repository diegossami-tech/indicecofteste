# Estado Atual

## O que já está dentro do projeto

- skill original em `skills/filosofia-olavo-cof/`;
- script original de compactação preservado em `scripts/criar-zip-original.ps1`;
- documentação inicial de projeto;
- scripts novos para validação e empacotamento;
- configuração local do corpus em `config/corpus.local.json`;
- scripts para validar e pesquisar o TXT completo;
- interface web local em `web/` para consulta no navegador.

## Observações

- o conteúdo central da skill não foi reescrito nesta etapa;
- durante a leitura no terminal apareceram caracteres com aparência de encoding incorreto, então vale validar os arquivos em um editor com suporte a UTF-8;
- os índices documentam vários gaps de OCR e resumos ainda pendentes.
